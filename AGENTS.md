# Caffeinix web agent notes

## Patch series

- Use Linux-style subjects: `<subsystem>: <imperative summary>`.
- Wrap commit messages at 72 columns.
- Add a DCO `Signed-off-by` trailer to every patch.
- Keep patches focused and buildable.  Present the final design directly.
- Run unit, browser runtime, responsive layout, and whitespace checks before
  handoff.
- Do not push or open a pull request until the user explicitly approves.
